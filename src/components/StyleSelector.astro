<dialog class="dark:bg-custom-bg-dark bg-white" >
 <h2 class="text-5xl dark:text-white semibold flex items-center">Selecciona un estilo</h2>
  <form method="dialog" id="style-selector-form"></form>
</dialog>
<style>
  dialog {
    transition: scale 0.3s ease;
    scale: 0;
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    display: flex;
    flex-direction: column;
    z-index: 10;
    border-radius: 8px;
    padding: 32px;
    gap: 32px;

    > h2{
      width: 100%;
      height: 96px;
    }

    >form{
      height: 100%;
      padding: 32px;
      background-color: rgba(128, 128, 128, 0.226);
      border-radius: 16px;
      box-shadow: 5px 5px 10px rgba(0, 0, 0, 0.2) inset;
      overflow-y: auto;
      overflow-x: hidden;
      display: flex;
      flex-direction: column;
      gap: 16px;
    }

    &[open] {
      scale: 1;

      @starting-style {
        scale: 0;
      }
    }
  }
  #style-selector-button {
    color: black;
    padding: 10px 20px;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    background-color: aqua;
  }

  @keyframes scale {
    0% {
      transform: scale(0.5);
      opacity: 0;
      visibility: hidden;
    }
    100% {
      transform: scale(1);
      opacity: 1;
      visibility: visible;
    }
  }

  /* Animación para ocultar (desaparecer con escala) */
  @keyframes display {
    0% {
      transform: scale(1);
      opacity: 1;
      visibility: visible;
    }
    100% {
      transform: scale(0.5);
      opacity: 0;
      visibility: hidden;
    }
  }
</style>
<script>
  
  const selectorButton = document.getElementById("style-selector-button");
  if (selectorButton) {
    selectorButton.addEventListener("click", () => {
      const dialog = document.querySelector("dialog");
      dialog.showModal();
    });
  }

  const styleCard = document.getElementById("style-card");
  if (styleCard) {
    styleCard.addEventListener("click", () => {
      const dialog = document.querySelector("dialog");
      dialog.close();
    });
  }
</script>
