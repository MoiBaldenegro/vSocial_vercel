const previewExecute = new Promise((resolve, reject) => {});

/*

  const previewResponse = transformerModel(public_id, promt); // instancia con la public-ip
      previewResponse.getImageUrl();
      previewResponse.renderPreview();

      */
